import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    header: {
        background: '#333'
    },
    landing: {
        background: '#00466E'
    },
    projects: {
        background: '#E59F71'
    },
    contact: {
        background: '#69DC9E'
    },

});

export default theme;